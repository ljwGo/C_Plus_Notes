#include <iostream>
// 指定名称空间, 避免自己写的函数和引入的库冲突
using namespace std;

int main(void){
	// cout, iostream库中, 将数据导入到标准的输出设备(IO), window规定为显示器
	// endl(l是字母不是数字)是换行符 , << 表示插入
	cout << "hello world!" << endl;
	cout << "welcome to c++" << endl;
	return 0;
}